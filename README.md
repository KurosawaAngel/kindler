A little code to practice building code architecture.

Solves the issue where Kindle takes the book title from the filename, not the metadata. If the filename contains characters prohibited by the filesystem, maintaining name consistency becomes problematic.
